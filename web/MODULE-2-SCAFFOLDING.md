# Module-2 scaffolding — NOT LIVE, NOT REBRANDED

This `web/` directory is PolyParlay's website ported wholesale as scaffolding
for HypeLens Module 2 (wallet intel + shareable card viewer pages). Nothing
here is deployed for HypeLens and all copy/branding still says PolyParlay.

Planned reuse when Module 2 starts:

- `index.html`   -> hypelens landing (install CTA + trust posture)
- `slip.html`    -> shared funding-card viewer (decodes URL hash)
- `privacy.html` -> CWS-required privacy policy (rewrite: no data collection)
- `leaderboard.html`, `upgrade.html`, `vercel.json` -> evaluate then rebrand or delete

Do not deploy as-is.
